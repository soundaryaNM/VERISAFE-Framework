# VERISAFE CI Pipeline — triggers on every push
name: VERISAFE CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: "3.11"
  SAFETY_LEVEL: "SIL0"

jobs:
  # ... (full content is in the ci.yml file provided)